//! byreal-strategies has no upstream API — it is pure strategy rendering. The
//! only thing here is the app marker the tool registrations point at.

#[derive(Clone, Default)]
pub struct ByrealStrategiesApp;
